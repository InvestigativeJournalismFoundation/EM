# Ditto config-driven ER pipeline + FAIR-DA4ER Ditto (ditto_light) training stack.
# Install: pip install -r requirements.txt
#
# Optional: use a virtual environment first:
#   python -m venv .venv && source .venv/bin/activate   # Linux/macOS
#   pip install --upgrade pip

# --- Pipeline & configs ---
pandas
numpy
pyyaml
scikit-learn
scipy
psycopg2-binary
python-dotenv
boto3

# --- PyTorch & Hugging Face (train / test / SBERT / ANN embeddings) ---
# In Docker, torch and torchvision are pre-installed from the PyTorch CUDA index
# (see Dockerfile) so these are effectively no-ops there. For local CPU installs,
# pip pulls them from PyPI.
torch
torchvision
transformers
sentence-transformers
tqdm

# --- FAISS: required only for blocking strategy `ann` (configs/blocking.yaml) ---
faiss-cpu

# --- FAIR-DA4ER / Ditto original training stack (ditto_light, notebooks, etc.) ---
gensim
jsonlines
nltk
protobuf
regex
sentencepiece
spacy
tensorboardX
timm
wheel
wordfreq

# --- Optional: er_pipeline.analysis plots ---
matplotlib
